# Qarar API Documentation

The API contract is split by domain under [`api/`](./api/README.md).

Start with the API index for authentication, conventions, endpoint groups, permissions, examples,
and the implementation status of every operation. Do not add endpoint details to this file; add
them to the relevant domain document so the contract remains reviewable.
