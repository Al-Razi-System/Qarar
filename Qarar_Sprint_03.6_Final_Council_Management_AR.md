<div dir="rtl" style="text-align: right;">

# منصة قرار

## Sprint 03.6 — إدارة المجالس

**الحالة:** معتمد للتنفيذ  
**الإصدار:** 1.1 نهائي معتمد  
**التاريخ:** 29 يوليو 2026  
**النطاق:** Backend وSupabase و`api_v1` والترحيل والاختبارات والتوثيق  
**الموضع:** بعد Sprint 03.5 وقبل Sprint 04  
**بوابة الخروج:** إنشاء مجلس وإدارة بياناته وهيكله وأعضائه وقيادته وحالته بالكامل دون كتابة مباشرة في الجداول

---

## 1. الغرض

يعالج Sprint 03.6 فجوة إدارة المجالس داخل منصة قرار.

النظام يحتوي أصلًا على جداول تمثل الوحدات الحوكمية والعضويات والأدوار والتصنيفات، لكنه لا يوفر رحلة إدارية مكتملة ومحكومة عبر `api_v1` من أجل:

- إنشاء مجلس.
- تعديل بيانات المجلس.
- عرض المجالس والبحث فيها.
- بناء هيكل المجالس.
- تعيين الأعضاء.
- تعيين رئيس المجلس ومقرر المجلس.
- فحص الاكتمال الإداري للمجلس.
- تفعيل المجلس أو تعطيله أو أرشفته.
- تنفيذ كل ذلك دون SQL مباشر أو Seed Data يدوية.

لا يعيد Sprint بناء الأنظمة السابقة، بل يضيف طبقة إدارة المجالس الناقصة فوق النموذج الحالي.

---

## 2. خط الأساس الحالي

### 2.1 الموجود والمنفذ

| القدرة | الحالة الحالية |
|---|---|
| أنواع الوحدات | موجودة في `qarar_core.governance_unit_types` |
| الوحدات الحوكمية | موجودة في `qarar_core.governance_units` |
| علاقة الأب | موجودة عبر `parent_unit_id` |
| حالات الوحدة | `active`, `inactive`, `archived` |
| عزل المؤسسة | موجود عبر `organization_id` والمفاتيح المركبة |
| العضويات | موجودة في `qarar_iam.memberships` |
| الأدوار | موجودة في `qarar_iam.roles` |
| تصنيف المجلس | موجود عبر `governance_class_id` |
| تعيين التصنيف | مدعوم عبر العقد الحالي |

### 2.2 الفجوات

| القدرة | الفجوة |
|---|---|
| إنشاء المجلس | لا توجد عقود إدارة محكومة |
| تعديل المجلس | لا توجد عقود مخصصة |
| البحث والتفاصيل | لا توجد واجهة API مستقرة |
| شجرة المجالس | لا يوجد عقد مخصص |
| تغيير الأب | لا توجد حماية كافية من العلاقات الدائرية |
| إدارة الأعضاء | تعتمد على عقود IAM عامة وليست رحلة مجلس متكاملة |
| تعيين القيادة | لا توجد عملية ذرية للرئيس والمقرر |
| الاكتمال الإداري | لا يوجد عقد موحد |
| الحالة | لا توجد انتقالات إدارية كاملة ومدققة |
| توثيق الواجهة | لا توجد عقود جاهزة لتسليم Flutter |

---

## 3. قرارات التصميم النهائية

1. لا ينشأ جدول جديد باسم `councils`.
2. يبقى `qarar_core.governance_units` مصدر الحقيقة للمجالس.
3. المجلس هو سجل داخل `governance_units` من نوع مجلس أو لجنة.
4. لكل مجلس تصنيف حوكمي رئيسي واحد في `governance_class_id`.
5. يبقى `level_no` عمقًا تقنيًا يحسبه الخادم.
6. تبقى حالات المجلس: `inactive`, `active`, `archived`.
7. لا تضاف حالات جديدة مثل `draft` أو `suspended`.
8. لا يوجد حذف مادي للمجلس.
9. تستخدم العضويات الحالية في `qarar_iam.memberships`.
10. تبقى حالات العضوية المخزنة: `active`, `inactive`, `ended`.
11. تمثل القيادة عبر الدورين `council_chair` و`council_rapporteur`.
12. لا ينشأ جدول مستقل للقيادة.
13. لا يضاف جدول `governance_unit_terms`.
14. لا تضاف دورة تشكيل مستقلة للمجلس.
15. لا يدخل SSO في نطاق Sprint.
16. لا تدخل اللوائح أو تقارير التغطية في نطاق Sprint.
17. لا تدخل سياسات الاجتماعات أو الحضور أو النصاب أو التصويت أو المحاضر في نطاق Sprint.
18. تبقى أي أعمدة حالية تخص هذه السياسات دون تعديل أو تعريض في عقود إدارة المجلس.
19. كل كتابة من الواجهة تمر عبر `api_v1`.
20. لا يستقبل العميل `organization_id`.
21. تستخدم التحديثات الحساسة Optimistic Concurrency.
22. تستخدم الإنشاءات Idempotency.
23. لا تغير إدارة المجلس أي Snapshot تاريخي قائم.

---

## 4. حدود النطاق

### 4.1 داخل النطاق

- إدارة أنواع المجالس.
- إنشاء المجلس وتعديل بياناته.
- البحث في المجالس وقراءة التفاصيل.
- توفير عقد قراءة مرجعي عام للكيانات والعمليات التي تتطلب اختيار مجلس.
- عرض شجرة المجالس ونقل المجلس داخل الهيكل.
- منع العلاقات الدائرية.
- ربط المجلس بتصنيف حوكمي.
- إدارة أعضاء المجلس.
- تعيين الرئيس والمقرر.
- إنهاء العضوية دون حذف.
- تحديد الحد الأدنى للأعضاء.
- فحص الاكتمال الإداري للمجلس.
- التفعيل والتعطيل والأرشفة.
- صلاحيات إدارة المجالس.
- Audit Log.
- Migration وBackfill.
- اختبارات قاعدة البيانات وHTTP.
- توثيق عقود الواجهة.

### 4.2 خارج النطاق

- دورة تشكيل المجلس وجدول `governance_unit_terms`.
- SSO وSCIM وHR Integration.
- إدارة المستخدمين والأدوار العامة.
- إنشاء اللوائح أو تعديلها.
- تقارير وتنبيهات التغطية.
- Outbox خاص بالتغطية.
- إنشاء الاجتماعات والحضور والتصويت.
- سياسات الاجتماعات والحضور والنصاب والتصويت.
- المحاضر واعتمادها.
- القرارات والتنفيذ.
- الانتخابات والمكافآت والبدلات.
- مجالس مشتركة بين مؤسسات مختلفة.
- تعدد التصنيفات للمجلس الواحد.
- تنفيذ شاشات Flutter.

---

## 5. المصطلحات

| المصطلح | التعريف |
|---|---|
| المجلس | جهة مؤسسية تضم رئيسًا ومقررًا وأعضاء. |
| اللجنة | جهة مشابهة للمجلس ويمكن إدارتها بالآلية نفسها. |
| الوحدة الحوكمية | الاسم التقني العام للسجل داخل `governance_units`. |
| نوع المجلس | النوع الذي يحدد أن الوحدة تمثل مجلسًا أو لجنة. |
| المجلس الأب | الجهة التي يتبع لها المجلس داخل الهيكل. |
| التصنيف الحوكمي | التصنيف الذي يحدد المستوى الحوكمي للمجلس. |
| العضوية | ارتباط مستخدم بدور داخل مجلس خلال فترة محددة. |
| القيادة | رئيس المجلس ومقرر المجلس. |
| الاكتمال الإداري | نتيجة تحدد هل بيانات المجلس وهيكله وعضوياته وقيادته مكتملة للتفعيل. |
| العضوية الفعلية | عضوية نشطة بدأ تاريخها ولم ينتهِ. |

---

## 6. نموذج البيانات

### 6.1 `qarar_core.governance_unit_types`

تضاف الحقول:

| الحقل | النوع | الغرض |
|---|---|---|
| `is_council_type` | boolean | هل يسمح النوع بإنشاء مجلس أو لجنة |
| `is_system` | boolean | حماية النوع النظامي |

القواعد:

- إنشاء مجلس يتطلب نوعًا فعالًا و`is_council_type = true`.
- النوع النظامي لا يحذف.
- النوع المستخدم في مجلس فعال لا يعطل.
- الرمز فريد داخل المؤسسة.

### 6.2 `qarar_core.governance_units`

تبقى الحقول الحالية، ومنها:

```text
id
organization_id
parent_unit_id
unit_type_id
governance_class_id
code
name_ar
name_en
level_no
status
created_at
updated_at
```

تضاف:

| الحقل | النوع | الغرض |
|---|---|---|
| `description` | text | وصف المجلس |
| `status_reason` | text | سبب التعطيل أو الأرشفة |
| `status_changed_at` | timestamptz | وقت آخر تغيير للحالة |
| `status_changed_by_user_id` | uuid | منفذ آخر تغيير |
| `activated_at` | timestamptz | تاريخ التفعيل |
| `archived_at` | timestamptz | تاريخ الأرشفة |
| `minimum_active_members` | integer | الحد الأدنى للأعضاء |
| `allow_dual_leadership` | boolean | السماح للشخص نفسه بأن يكون رئيسًا ومقررًا |

القواعد:

- `code` فريد داخل المؤسسة.
- صيغة الرمز: `^[a-z][a-z0-9_]*$`، مثل `ai_department_council`.
- `minimum_active_members >= 1`.
- الأب والنوع والتصنيف من المؤسسة نفسها.
- المجلس لا يكون أبًا لنفسه ولا ينقل تحت أحد أحفاده.
- الخادم يحسب `level_no` ولا يقبله من العميل.
- لا يغير نقل المجلس أي سجل تاريخي.

### 6.3 `qarar_iam.memberships`

لا ينشأ جدول جديد.

تعد العضوية فعالة عندما:

```text
membership_status = 'active'
AND start_date <= effective_date
AND (end_date IS NULL OR end_date >= effective_date)
```

| الحالة المعروضة | الشرط |
|---|---|
| `scheduled` | العضوية `active` والبداية مستقبلية |
| `effective` | العضوية `active` والتاريخ داخل الفترة |
| `expired` | انتهت فترة العضوية |
| `inactive` | العضوية مخزنة `inactive` |
| `ended` | العضوية مخزنة `ended` |

قواعد العضوية:

- المستخدم والمجلس والدور من المؤسسة نفسها.
- الدور فعال ومناسب لنطاق المجلس.
- تاريخ النهاية لا يسبق البداية.
- العضوية المستقبلية أو المنتهية لا تمنح صلاحية.
- العضوية لا تحذف؛ الإنهاء يحولها إلى `ended` مع سبب إلزامي.
- لا يغير المستخدم أو الدور بعد بدء العضوية.
- يمنع تداخل عضويتين للمستخدم والدور والمجلس نفسه.

### 6.4 قيادة المجلس

الرئيس: `council_chair`  
المقرر: `council_rapporteur`

القواعد:

- لكل مجلس رئيس فعال واحد على الأكثر.
- لكل مجلس مقرر فعال واحد على الأكثر.
- تعيين الرئيس والمقرر يتم ذريًا.
- لا يسمح للشخص نفسه بالدورين عندما `allow_dual_leadership = false`.
- تغيير القيادة يتطلب سببًا.
- القيادة السابقة لا تحذف، بل تنتهي وتحفظ تاريخيًا.

### 6.5 سجل حالة المجلس

ينشأ:

```text
qarar_core.governance_unit_status_history
```

الحقول:

```text
id
organization_id
governance_unit_id
from_status
to_status
reason
changed_by_user_id
changed_at
```

السجل Append-only ويكتب في المعاملة نفسها مع تغيير الحالة.

---

## 7. هيكل المجالس

```text
مجلس الجامعة
├── مجلس كلية الحاسوب
│   ├── مجلس قسم الذكاء الاصطناعي
│   └── مجلس قسم تقنية المعلومات
└── مجلس كلية العلوم الإدارية
```

القواعد:

- يمكن أن يكون المجلس جذرًا.
- يمكن أن يتبع مجلسًا أو جهة داخل المؤسسة.
- يمنع Self-parent وأي Cycle مباشرة أو غير مباشرة.
- لا يسمح بأب من مؤسسة أخرى.
- نقل المجلس يعيد حساب `level_no` له ولجميع أحفاده.
- عملية النقل ذرية وتستخدم Optimistic Concurrency.
- يمنع نقل مجلس مؤرشف.
- يسجل الأب السابق والجديد في Audit.

---

## 8. دورة حياة المجلس

الحالات:

```text
inactive
active
archived
```

الانتقالات:

```text
inactive -> active
active   -> inactive
inactive -> archived
active   -> archived
archived -> لا انتقال
```

- `inactive`: تحت الإعداد أو معطل، ولا يستخدم في عمليات جديدة.
- `active`: مكتمل إداريًا ومتاح كمرجع للكيانات والعمليات الأخرى.
- `archived`: سجل تاريخي نهائي لا يعاد للخدمة.
- التعطيل والأرشفة يتطلبان سببًا.

---

## 9. الاكتمال الإداري للمجلس

يعد المجلس مكتملًا إداريًا عندما:

- النوع موجود وفعال ومعرف كنوع مجلس.
- الأب صالح ولا توجد Cycle.
- التصنيف الحوكمي موجود وفعال.
- الحد الأدنى للأعضاء صالح.
- عدد الأعضاء الفعليين يساوي أو يتجاوز الحد الأدنى.
- يوجد رئيس فعلي ومقرر فعلي.
- لا يوجد أكثر من رئيس أو مقرر فعال.
- لا يجمع شخص واحد الدورين عندما يكون ذلك ممنوعًا.

الاستجابة:

```json
{
  "governance_unit_id": "<uuid>",
  "administratively_ready": false,
  "errors": [
    {
      "code": "chair_required",
      "field": "leadership",
      "message": "يجب تعيين رئيس فعال للمجلس"
    }
  ],
  "warnings": [],
  "checked_at": "<timestamp>"
}
```

رموز الأخطاء:

```text
unit_type_required
unit_type_inactive
unit_type_not_council
invalid_parent
hierarchy_cycle
governance_class_required
governance_class_inactive
invalid_minimum_active_members
minimum_active_members_not_met
chair_required
rapporteur_required
multiple_active_chairs
multiple_active_rapporteurs
dual_leadership_not_allowed
```

لا يعني الاكتمال الإداري أن المجلس مستوفٍ لشروط أي عملية أخرى. لا تدخل
التغطية اللائحية أو سياسات الاجتماعات أو الحضور أو النصاب أو التصويت أو
المحاضر ضمن هذه النتيجة. يطبق كل كيان مستهلك متطلباته الخاصة بصورة مستقلة.

---

## 10. الصلاحيات

| الصلاحية | النطاق | الغرض |
|---|---|---|
| `governance.units.read` | organization / unit | عرض المجالس والشجرة |
| `governance.units.manage` | organization | إنشاء وتعديل المجلس |
| `governance.units.activate` | organization | التفعيل والتعطيل |
| `governance.units.archive` | organization | الأرشفة |
| `governance.unit_types.manage` | organization | إدارة أنواع المجالس |
| `governance.memberships.read` | unit | عرض أعضاء المجلس |
| `governance.memberships.manage` | unit | إدارة العضويات |
| `governance.leadership.assign` | unit | تعيين القيادة |

لا يعاد بناء IAM؛ المطلوب إضافة الصلاحيات الجديدة وربطها بالأدوار المناسبة فقط.

---

## 11. قواعد عقود `api_v1`

كل العقود تستدعى عبر:

```text
POST /rest/v1/rpc/<contract_name>
```

القواعد:

- المؤسسة تستمد من سياق المستخدم.
- لا يقبل `organization_id` من العميل.
- البحث يعيد `items`, `total`, `limit`, `offset`.
- الحد الافتراضي 25 والأقصى 100.
- الإنشاءات الحرجة تقبل `p_client_request_id`.
- التحديثات تقبل `p_expected_updated_at`.
- لا تعاد رسائل PostgreSQL الخام.
- الخطأ المنظم يحتوي `code`, `message`, `field`, `details`, `request_id`.
- لا توجد كتابة مباشرة للجداول من Flutter.

---

## 12. العقود المطلوبة

### 12.1 خيارات النماذج

```text
get_council_form_options
```

يعيد:

```text
council_types
parent_units
governance_classes
leadership_roles
```

### 12.2 أنواع المجالس

```text
admin_search_council_types
admin_create_council_type
admin_update_council_type
admin_deactivate_council_type
```

### 12.3 إدارة المجالس

```text
admin_search_councils
admin_get_council_detail
admin_create_council
admin_update_council
```

مثال إنشاء:

```json
{
  "p_code": "ai_department_council",
  "p_name_ar": "مجلس قسم الذكاء الاصطناعي",
  "p_name_en": "AI Department Council",
  "p_description": null,
  "p_unit_type_id": "<uuid>",
  "p_parent_unit_id": "<uuid-or-null>",
  "p_governance_class_id": "<uuid>",
  "p_minimum_active_members": 5,
  "p_allow_dual_leadership": false,
  "p_client_request_id": "<uuid>"
}
```

ينشأ المجلس بالحالة `inactive`.

### 12.4 الاستهلاك المرجعي

```text
get_available_councils
```

يعيد المجالس الفعالة داخل المؤسسة، مع مرشحات اختيارية للنوع والتصنيف والأب
والبحث النصي. يستخدمه أي كيان أو عملية تحتاج مرجع مجلس، ولا يمنح أي صلاحية
على العملية المستهلكة نفسها. يقرر كل كيان مستهلك شروطه الإضافية وصلاحياته.

### 12.5 شجرة المجالس

```text
admin_get_councils_tree
admin_move_council
```

### 12.6 أعضاء المجلس

```text
admin_list_council_members
admin_add_council_member
admin_update_council_membership
admin_end_council_membership
```

### 12.7 قيادة المجلس

```text
admin_assign_council_leadership
```

### 12.8 الاكتمال الإداري والحالة

```text
admin_validate_council_administrative_readiness
admin_activate_council
admin_deactivate_council
admin_archive_council
```

---

## 13. تدفقات الاستخدام

### إنشاء مجلس

```text
تحميل أنواع المجالس
↓
اختيار الأب والتصنيف
↓
إنشاء المجلس inactive
↓
إضافة الأعضاء
↓
تعيين الرئيس والمقرر
↓
فحص الاكتمال الإداري
↓
معالجة الأخطاء
↓
تفعيل المجلس
```

### تغيير القيادة

```text
قراءة القيادة الحالية
↓
اختيار القيادة الجديدة
↓
إدخال تاريخ النفاذ والسبب
↓
إنهاء القيادة السابقة
↓
إنشاء القيادة الجديدة ذريًا
↓
إعادة تقييم الاكتمال الإداري
```

---

## 14. Audit

الأحداث المطلوبة:

```text
council.type.created
council.type.updated
council.type.deactivated
council.created
council.updated
council.parent_changed
council.class_changed
council.activated
council.deactivated
council.archived
council.membership.added
council.membership.updated
council.membership.ended
council.leadership.changed
council.administrative_readiness.checked
```

لا ينفذ Outbox جديد في هذا Sprint.

---

## 15. الترحيل

خطوات الترحيل:

1. إضافة أعمدة أنواع المجالس.
2. إضافة أعمدة المجلس.
3. إنشاء سجل الحالة.
4. إضافة القيود والفهارس.
5. إضافة الصلاحيات وربطها بالأدوار.
6. تصحيح فحوص العضوية الزمنية.
7. اكتشاف المجالس الحالية.
8. إنشاء سجل حالة ابتدائي.
9. تحديث Entity Registry وAPI Contract Registry وRelease Lock.
10. تشغيل اختبار الترقية.

لا ينفذ الترحيل:

- إنشاء دورات للمجالس.
- تخمين تواريخ تشكيل.
- تعديل الاجتماعات أو اللوائح.
- إنشاء تقارير تغطية.
- تغيير Snapshots تاريخية.

تقرير ما قبل الترحيل يكشف الرموز المكررة، العلاقات العابرة للمؤسسات،
العلاقات الدائرية، الأنواع غير الواضحة، التصنيفات المفقودة، القيادة المكررة،
والعضويات الزمنية غير الصحيحة.

---

## 16. الاختبارات

### قاعدة البيانات

- إنشاء مجلس ومنع الرمز المكرر.
- منع النوع أو الأب أو التصنيف من مؤسسة أخرى.
- منع Self-parent وجميع أشكال Cycle.
- حساب `level_no` ونقل مجلس له أحفاد.
- Rollback كامل عند الفشل.
- إضافة عضو ومنع التداخل.
- منع العضوية المستقبلية والمنتهية من الصلاحية.
- تعيين القيادة ذريًا ومنع التكرار.
- تطبيق `allow_dual_leadership`.
- فحص الاكتمال الإداري ومنع التفعيل عند وجود أخطاء.
- انتقالات الحالة.
- Idempotency وOptimistic Concurrency.
- Cross-Tenant Isolation وAudit atomicity.

### HTTP

- إنشاء مجلس كامل عبر HTTP.
- البحث والتفاصيل والشجرة والنقل.
- إضافة الأعضاء وتعيين القيادة.
- فحص الاكتمال الإداري وتغيير الحالة.
- التفعيل والتعطيل والأرشفة.
- Pagination وStable JSON وStructured Errors.
- Idempotent Replay وConcurrency Conflict.
- Cross-Tenant denial.

### الترحيل

- Fresh reset.
- Upgrade من قاعدة قائمة.
- عدم فقد البيانات أو تغيير السجلات التاريخية.
- Migration atomicity وchecksum.
- API Registry parity وDocumentation parity.

---

## 17. عناصر العمل

| المعرف | المهمة | الأولوية |
|---|---|---|
| PB-072 | نموذج البيانات والترحيل الأساسي | P0 |
| PB-073 | إدارة أنواع المجالس | P0 |
| PB-074 | إنشاء المجالس والبحث والتفاصيل والتعديل والاستهلاك المرجعي | P0 |
| PB-075 | شجرة المجالس ونقلها ومنع العلاقات الدائرية | P0 |
| PB-076 | إدارة أعضاء المجلس وفترات العضوية | P0 |
| PB-077 | تعيين رئيس المجلس ومقرره وتغيير القيادة | P0 |
| PB-078 | الاكتمال الإداري للمجلس والحالة | P0 |
| PB-079 | الصلاحيات والتدقيق والتوثيق والاختبارات | P0 |

---

## 18. معايير قبول المهام

### PB-072

- Migration جديدة غير معدلة للتاريخ.
- Fresh install وUpgrade ناجحان.
- لا فقد للبيانات ولا كسر للعقود الحالية.

### PB-073

- إدارة أنواع المجالس دون SQL.
- حماية النوع النظامي ومنع تعطيل النوع المستخدم.
- عزل المؤسسة وAudit وConcurrency.

### PB-074

- إنشاء المجلس `inactive`.
- بحث وPagination مستقران.
- عقد `get_available_councils` صالح لإعادة الاستخدام ولا يتجاوز صلاحيات الكيان المستهلك.
- Idempotency وOptimistic Concurrency وTenant Isolation.

### PB-075

- منع جميع أشكال Cycle.
- تحديث المستويات ذريًا.
- تسجيل الأب السابق والجديد.

### PB-076

- العضوية المستقبلية أو المنتهية لا تمنح صلاحية.
- منع التداخل وحفظ التاريخ.
- عدم تغيير المستخدم أو الدور بعد بدء العضوية.

### PB-077

- تعيين الرئيس والمقرر ذريًا.
- منع القيادة المكررة.
- تطبيق `allow_dual_leadership`.
- حفظ القيادة السابقة وطلب سبب للتغيير.

### PB-078

- كل خطأ اكتمال إداري له رمز مستقر.
- لا تفعيل مع أخطاء.
- الأرشفة نهائية.
- History وAudit ذريان.

### PB-079

- إضافة الصلاحيات وProvisioning للمؤسسات الجديدة.
- تسجيل العقود في API Registry.
- توثيق كامل وتسليم واضح للواجهة.
- نجاح اختبارات DB وHTTP والترقية.

---

## 19. بوابة البدء

يبدأ Sprint بعد:

- وجود Sprint 03.5 في `dev`.
- اعتماد هذه الوثيقة.
- مراجعة مخطط `governance_units` و`memberships`.
- تثبيت أسماء العقود.

---

## 20. بوابة الخروج

لا يعد Sprint مكتملًا إلا إذا:

1. يمكن إنشاء مجلس عبر `api_v1`.
2. يمكن تعديل بياناته وربطه بالأب والتصنيف.
3. يمكن عرض شجرة المجالس ومنع العلاقات الدائرية.
4. يمكن إضافة الأعضاء مع تطبيق فترات العضوية.
5. يمكن تعيين رئيس ومقرر دون قيادة مكررة.
6. يمكن فحص الاكتمال الإداري ولا يفعل مجلس غير مكتمل إداريًا.
7. يمكن تعطيل المجلس وأرشفته.
8. تحفظ جميع التغييرات في Audit.
9. تحترم جميع العقود عزل المؤسسة.
10. لا توجد كتابة مباشرة من Flutter.
11. تنجح الترقية والاختبارات.
12. لا يتضمن Sprint SSO أو اللوائح أو التغطية أو سياسات الاجتماعات أو الحضور أو النصاب أو التصويت أو المحاضر.
13. جميع العقود مسجلة في `api_v1` وموثقة.

---

## 21. السيناريو المرجعي

```text
إنشاء نوع مجلس قسم
↓
إنشاء مجلس قسم الذكاء الاصطناعي inactive
↓
ربطه بمجلس كلية الحاسوب
↓
تعيين التصنيف الحوكمي
↓
إضافة أعضاء المجلس
↓
تعيين رئيس المجلس ومقرره
↓
تحديد الحد الأدنى للأعضاء
↓
فحص الاكتمال الإداري
↓
معالجة الأخطاء
↓
تفعيل المجلس
```

ينفذ السيناريو كاملًا دون SQL مباشر أو دورة تشكيل أو SSO أو لائحة أو اجتماع أو محضر.

---

## 22. مخرجات التوثيق

- `supabase/docs/api/16-councils-management.md`
- `supabase/docs/api/12-contract-reference.md`
- `supabase/docs/api/14-json-response-contracts.md`
- `supabase/docs/api/13-frontend-integration-guide.md`
- `supabase/docs/guides/04-council-management-ar.md`
- `supabase/docs/architecture/README.md`
- `docs/07_Data_Model_Qarar_Arabic.md`
- `docs/19_API_Specification_Qarar_Arabic.md`

يتضمن توثيق كل عقد الصلاحية المطلوبة، Payload، الاستجابة، Pagination، Idempotency، Optimistic Concurrency، رموز الأخطاء، الحالات المانعة، ومثال نجاح وفشل.

---

## 23. تعريف الاكتمال

لا يعد أي عنصر مكتملًا إلا إذا:

- نفذ عبر Migration جديدة.
- استخدم `api_v1`.
- سجل في السجلات المعمارية.
- اشتق المؤسسة والمستخدم من سياق موثوق.
- اختبر Cross-Tenant وRBAC.
- اختبر الحالات السلبية والتزامن.
- حافظ على البيانات الحالية.
- حدث توثيق API.
- نجحت اختبارات قاعدة البيانات وHTTP والترقية.

ولا يعد Sprint 03.6 مكتملًا بمجرد وجود الجداول أو نجاح Seed Data.

---

## 24. الخلاصة

يعالج Sprint 03.6 فجوة واحدة محددة:

> تمكين المؤسسة من إنشاء مجالسها وإدارة بياناتها وهيكلها وأعضائها وقيادتها وحالتها من خلال عقود إنتاجية محكومة.

لا يعيد Sprint بناء IAM، ولا يضيف دورة مجلس، ولا ينفذ SSO، ولا يتولى اللوائح أو التغطية أو الاجتماعات أو التصويت أو المحاضر.

---

**نهاية وثيقة Sprint 03.6 — إدارة المجالس**

</div>
