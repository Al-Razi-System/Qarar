# Supabase local development
supabase/.branches/
supabase/.temp/
supabase/.env
supabase/.env.*
!supabase/.env.example

# General local files
node_modules/
.production-reports/
backups/
*.dump
*.dump.sha256
.codex-runlogs/
test-results/
tmp/
/admin/
.next/
playwright-report/
*.log
.env
.env.*
!.env.example
!deploy/production/.env.production.example

# Approved initial-admin requests are operational records and may contain PII.
# Keep the committed example only; create the real request outside the checkout.
deploy/production/initial-admin-bootstrap.json
